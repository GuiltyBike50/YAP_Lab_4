#include <iostream>
#include <string>
using namespace std;

class String
{
private:
    char *value;
    int len;
public:
    String()
    {
        value = new char[1];
        len = 0;
                   value[0] = '\0';
    }

    void Stroka(string str)
    {
        len = str.length();
        value = new char[len+1];
        for (int i=0; i<=len; i++) {
            value[i]=str[i];
        }
    }

    String(const String& s)
    {
        len=s.len;
        value = new char[len+1];
        for (int i=0; i<=len; i++) {
            value[i]=s.value[i];
        }
    }

    friend ostream& operator<<(ostream& outputStream, const String & s);

    ~String()
    {
        delete[] value;
    }

};

ostream& operator<<(ostream& outputStream, const String & s)
{
    return outputStream << s.value;
}

int main(int argc, char **argv)
{
    cout << "Введите строку" << endl;
    string stroka1;
    cin >> stroka1;
    String a;
    a.Stroka(stroka1);
    String b(a);
    String c{};

    cout << "Вывод строки с помощью перегруженного оператора <<" << endl;
    cout << a << endl;
    cout << "Вывод скопированной строки" << endl;
    cout << b << endl;
    cout << "Вывод строки по умолчанию" << endl;
    cout << c << endl;

    return 0;
}
